import { PostCards } from "./PostCards";
import { Post } from "./Post";
import { CreatePost } from "./CreatePost";

const routes = {
    cards: PostCards,
    post: Post,
    addCard: CreatePost,
}
export {routes};